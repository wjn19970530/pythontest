name: 获取验证码 api/account/GET_TrialVeriCode.yml
variables:
    trialPhoneNum: ${ENV(trialPhoneNum)}
    verifyCode: ${ENV(verifyCode)}
    endpoint: pc-web
base_url: ${ENV(base_url)}
request:
    headers:
        end-point: ${endpoint}
    method: GET
    params:
        phoneNum: ${trialPhoneNum}
    url: /uaaaccount/api/account/verify-code
validate:
    -   eq: ["status_code", 200]

