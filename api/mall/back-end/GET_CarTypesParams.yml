name: 查询车辆详细信息 api/mall/back-end/GET_CarTypesParams
variables:
    access_token: ${get_token(access_token)}
    brandId: ${get_value_from_tmp(brand_id)}
base_url: ${ENV(base_url)}
request:
    headers:
        Authorization: Bearer ${access_token}
    method: GET
    params:
        page: '0'
        size: '20'
        timestr: '1566814106928'
        brandId: ${brandId}
    url: /mall/api/v0.1/back-end/car-types/params
validate:
-   eq: [status_code, 200]
